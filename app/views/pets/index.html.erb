<div class="container mt-10">
    <h1>Pets List 🐶 </h1>
    <ul>
        <% @pets.each do |pet| %>
            <li>
                <%= link_to pet.name, pet_path(pet.id) %>
            </li>

        <% end %>
    </ul>

    <%= link_to "Add New Pet", new_pet_path %>
</div>